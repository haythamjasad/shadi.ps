# Admin Backend

This folder is kept to match the `project/frontend` and `project/backend` layout.

The current admin dashboard in `admin/frontend/` uses the shared Shadi and Store backends, so there is no separate admin backend code here yet.
