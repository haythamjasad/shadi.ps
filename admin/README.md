# Admin Workspace

This folder groups the admin project files.

- `frontend/` - the admin dashboard application
- `backend/` - reserved for a dedicated admin backend if one is added later

Today the admin dashboard talks to the shared APIs instead of its own backend service.
